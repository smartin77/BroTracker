{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build and Run",
            "type": "shell",
            "command": "cmake --build build && ./build/BroTrackerUI.exe",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
